# SYNTAX TEST "source.github-actions-workflow" "plain string wrapped over multiple lines"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    steps:
      - name: Test
        shell: pwsh
        run: $a = @(
            "Hello",
            "World"
          )
