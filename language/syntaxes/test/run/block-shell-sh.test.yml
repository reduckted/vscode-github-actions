# SYNTAX TEST "source.github-actions-workflow" "block scalar using `sh` shell"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    steps:
      - name: Test
        shell: sh
        run: |
          echo "Hello"
          exit 1
