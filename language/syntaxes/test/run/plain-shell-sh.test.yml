# SYNTAX TEST "source.github-actions-workflow" "plain string using `sh` shell"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    steps:
      - name: Test
        shell: sh
        run: echo "Hello"
