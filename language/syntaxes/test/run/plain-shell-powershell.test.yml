# SYNTAX TEST "source.github-actions-workflow" "plain string using `powershell` shell"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    steps:
      - name: Test
        shell: powershell
        run: Write-Host "Hello"
