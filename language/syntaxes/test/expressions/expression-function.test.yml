# SYNTAX TEST "source.github-actions-workflow" "function call in an expression"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step
        if: startsWith('test/', github.ref_name)
        uses: foo/bar@main
