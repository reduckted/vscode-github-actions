# SYNTAX TEST "source.github-actions-workflow" "null in expression"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: ${{ null == null }}
        uses: foo/bar@main
