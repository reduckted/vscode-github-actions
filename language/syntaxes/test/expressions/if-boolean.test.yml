# SYNTAX TEST "source.github-actions-workflow" "boolean in `if` property"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step
        if: true == false
        uses: foo/bar@main
