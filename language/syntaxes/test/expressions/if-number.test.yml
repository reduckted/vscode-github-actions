# SYNTAX TEST "source.github-actions-workflow" "number in `if` property"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step
        if: 1 == -2.3
        uses: foo/bar@main
