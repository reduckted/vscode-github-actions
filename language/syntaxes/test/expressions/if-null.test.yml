# SYNTAX TEST "source.github-actions-workflow" "null in `if` property"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step
        if: null == null
        uses: foo/bar@main
