# SYNTAX TEST "source.github-actions-workflow" "string in expression"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: ${{ 'foo bar' == 'bar foo' }}
        uses: foo/bar@main
