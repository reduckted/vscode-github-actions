# SYNTAX TEST "source.github-actions-workflow" "number in expression"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: ${{ 1 == -2.3 }}
        uses: foo/bar@main
