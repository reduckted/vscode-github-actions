# SYNTAX TEST "source.github-actions-workflow" "all values types in `if` property"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step
        if: success() || 'foo bar' == github.ref_name || 42 != true || null
        uses: foo/bar@main
