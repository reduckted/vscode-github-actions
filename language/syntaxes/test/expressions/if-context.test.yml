# SYNTAX TEST "source.github-actions-workflow" "context access in `if` property"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step
        if: github.ref_name == github.ref_name
        uses: foo/bar@main
