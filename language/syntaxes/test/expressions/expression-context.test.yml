# SYNTAX TEST "source.github-actions-workflow" "context access in an expression"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: ${{ github.ref_name }}
        uses: foo/bar@main
