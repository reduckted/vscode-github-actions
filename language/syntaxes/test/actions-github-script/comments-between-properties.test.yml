# SYNTAX TEST "source.github-actions-workflow" "comments between properties"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step
        uses: actions/github-script@v9
        # comment 1.
        with:
          # comment 2.
          script: |
            console.log("Hello");
