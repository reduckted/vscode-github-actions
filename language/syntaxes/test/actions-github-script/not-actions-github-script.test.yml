# SYNTAX TEST "source.github-actions-workflow" "not actions/github-script"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step
        uses: not-actions/github-script@main
        with:
          script: |
            console.log("Hello");
