# SYNTAX TEST "source.github-actions-workflow" "another input after `script` input when the script is a plain scalar"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step
        uses: actions/github-script@v9
        with:
          script: console.log("Hello");
          debug: true
