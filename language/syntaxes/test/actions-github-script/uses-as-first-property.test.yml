# SYNTAX TEST "source.github-actions-workflow" "`uses` as first property in step"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/github-script@v9 # ref
        with:
          script: |
            console.log("Hello");
