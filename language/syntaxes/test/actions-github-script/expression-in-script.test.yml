# SYNTAX TEST "source.github-actions-workflow" "the script contains a `${{ }}` expression"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step
        uses: actions/github-script@v9
        with:
          script: |
            console.log("${{ github.event_name }}");
          debug: true
