# SYNTAX TEST "source.github-actions-workflow" "another input before `script` input"

name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step
        uses: actions/github-script@v9
        with:
          debug: true
          script: |
            console.log("Hello");
